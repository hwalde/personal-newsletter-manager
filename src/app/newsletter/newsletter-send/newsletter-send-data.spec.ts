import { NewsletterSendData } from './newsletter-send-data';

describe('NewsletterSendData', () => {
  it('should create an instance', () => {
    expect(new NewsletterSendData()).toBeTruthy();
  });
});
